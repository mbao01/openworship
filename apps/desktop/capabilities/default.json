{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Default capability for OpenWorship",
  "windows": ["main", "display"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "dialog:default"
  ]
}
